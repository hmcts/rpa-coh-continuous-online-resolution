spring:
  cloud:
    propertiesvolume:
      enabled: true
      prefixed: true
      paths: /mnt/secrets/coh-cor
      aliases:
        microservicekey-coh-cor: S2S_TOKEN
        AppInsightsInstrumentationKey: APPINSIGHTS_INSTRUMENTATIONKEY
