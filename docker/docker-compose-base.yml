---
version: '2.1'

services:

  rd-coh-postgres-db:
      image: postgres:9.6
      environment:
      - POSTGRES_USER=cor
      - POSTGRES_PASSWORD=cor
      - POSTGRES_DB=cor
      ports:
      - 5432:5432
